# パピー業務ツール — システム概要

EC事業（株式会社パピー）の社内オペレーションを効率化する **Streamlit製の統合業務アプリ**。
受注変換・社内ナレッジ管理・請求書発行・買掛支払い・イベントページ作成まで、複数の定型業務を1つのWebアプリに集約している。

---

## 技術構成

| 項目 | 内容 |
|------|------|
| フレームワーク | Streamlit（Python） |
| デプロイ | Streamlit Cloud（GitHub連携・push で自動再デプロイ） |
| データベース | Notion API |
| ファイル/画像保存 | Google Drive（OAuth2） |
| AI | Anthropic Claude API（読取・要約・回答ドラフト・分析） |
| マスタ/履歴保管 | GitHub上のCSV・JSON |
| 認証情報 | Streamlit Secrets で一元管理 |

---

## 業務モジュール

### 🔧 1. 汎用マスタ変換
取引先ごとにバラバラな受注CSVを、**ネクストエンジン（NE）取込用の統一フォーマット**に変換する。

- 取引先ごとの項目マッピングをテンプレートとして保存・再利用
- JANコード／先方コードから自社商品コード・商品名を自動補完
- 個口数マスタによる配送個口の自動判定
- 商品コード空欄・未紐付けを変換時にエラー検知
- 出荷実績CSV生成、変換ログのダッシュボード表示

### 💬 2. AIナレッジ蓄積・質問管理
外注スタッフ（インハナ）と社内（パピー）のQ&Aを蓄積し、**会社の判断基準をAIに学習させる**仕組み。

- **質問投稿**：AIによる文章リライト、類似質問の自動チェック、タグ整合性チェック
- **回答管理**：AI回答ドラフト生成（過去事例を参照）、ステータス管理、検索、編集履歴
- **パターン分析**：蓄積データをAIが分析し、業務改善・再発防止の提案レポートを生成
- ステータス：未回答🔴 → ドラフト生成済🔵 → 回答済🟠 → 完了🟢

### 🧾 3. 請求書発行
作業料・保管料などの社内データから**請求書・見積書を発行**する。

- イレギュラー作業／保管カウントの実績入力
- 請求前のサニティチェック、マネーフォワード用CSV出力
- 発行履歴の保存と当時内容での再ダウンロード

### 💴 4. 請求書取込・買掛支払い
取引先から届く請求書を取り込み、**振込までを自動化**する。

- 請求書PDF/画像をClaude APIで読取（会社名・金額・口座・支払期日）
- 取引先マスタの口座と照合し、口座変更の可能性を注意喚起
- ネクストエンジンの発注データと金額突合
- 確認済みのものから楽天銀行 総合振込CSVを自動生成

### 📦 5. 資材備品チェック・発注
倉庫・現場の資材備品について**在庫チェック→発注要否判定→発注**までを行う。

- 資材マスタ（資材名・品番・仕入先・ロット・単価・発注点・在庫定数）を管理
- 仕入先は買掛の取引先マスタ（NE仕入先cd）と連携
- 有効資材を一括棚卸し、現在庫が**発注点以下**のものを「要発注」として抽出
- 発注数量は「(在庫定数−現在庫)をロット単位に切上げ」で提案（手修正可）、棚卸結果をNotionに保存
- ※Phase2で発注書（PDF/CSV）作成・メール送付に対応予定

### 🎪 6. イベントLP作成
楽天市場のイベント特集ページ（スーパーセール等）を**商品管理番号から自動生成し、楽天GOLDへFTP公開**する。

- 商品管理番号を入れるだけで商品名・価格・画像・レビューを自動取得（RMS Item API 2.0＋楽天市場商品検索API）
- 見出し付きコンテンツ（商品一覧／クーポン／ポイント変倍）を可変数で追加・並べ替え
- レスポンシブHTML（1ソース・外部JSなし）を生成し、PC/スマホプレビュー確認後にGOLDへアップ
- イベント定義はNotionに保存し、過去イベントの再編集・再利用が可能
- Streamlit CloudからFTPが通らない場合は `tools/gold_upload_local.py` でローカルアップ
- ※Phase2でクーポンAPI自動発行・ポイント変倍設定、Phase3でアクセス分析→次回イベントのたたき台AI生成に対応予定

### 💰 7. 価格改定
仕入価格（下代）の変更CSVから新販売価格を計算し、**楽天RMS・Yahoo!ショッピング・ネクストエンジンの価格更新CSVを一括生成**する。

- 計算ロジックはスプレッドシート「パピー納品価格変更」の数式を再現（変動費÷0.8＝利益率20%確保、3980円未満は送料込み換算）
- 価格ルール: 下代値下げ→据え置き／値上げ→「利益20%確保価格」と「値上げ率価格」の高い方（ルールは関数リストで拡張可能・指定価格/値上げ率の個別指定列にも対応）
- 送料・資材マスタは同シートの「送料表」「費用」タブから自動取得（フォールバックCSV同梱）
- NE商品マスタ（売価・原価入りNEカスタム）はDriveに保存し毎回アップ不要
- 納品価格変更／直送価格＆送料変更の2タブ（梱包サイズ変更は「📥 入荷登録」へ移設）

### 📥 8. 入荷登録
倉庫にはじめて入荷する商品の**ロケーション・配送サイズ登録と、それに伴うモール修正までをボタン一発で自動化**する（現場スタッフ用・CSV操作なし）。

- JANスキャン→商品マスタから商品コード・商品名・現登録内容を自動表示（最大10商品程度の一括入力）
- 資材ナンバー・ロケーション・配送サイズはプルダウンのみ（選択肢はNEマスタのロケーションコード既存値と送料・資材マスタから自動生成）
- NE商品マスタのロケーションコード（資材ナンバー-ロケーション）と項目1（配送サイズ）を**NE APIで自動更新**（api_v1_master_goods/upload・部分更新）
- 配送サイズが変わる場合は価格改定の「梱包サイズ変更」判定を流用: 便種変更→楽天配送方法セットをRMS APIで自動修正、サイズアップ利益NG→目標利益率価格に再設定し楽天価格もAPIで自動更新
- Yahooの価格・配送グループは当面CSV（Driveの「価格改定履歴/…_入荷登録」へ自動保存→管理者がまとめて反映。API化はYahoo審査通過後のPhase 4）
- 実行結果は行×ステップで表示し「失敗した処理だけ再実行」が可能。証跡一式をDriveに版数管理で保存

---

## このシステムの価値

- **属人化の解消**：判断基準やマッピングルールをデータとして蓄積・再利用
- **AI活用**：定型処理（変換・読取・回答作成・分析）をAIが補助し、人の判断に集中できる
- **一元化**：受注〜ナレッジ〜請求〜支払いを1つのツールで完結

---

*リポジトリ: kaburera0214-lab/hanyo-converter*
